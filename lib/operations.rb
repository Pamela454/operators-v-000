def unsafe?(speed)
  if speed < 40 || speed > 60
		return true
	end
else
	if speed > 40 || speed < 60
		return false
end
end


def not_safe?(speed)
if speed < 40 ? true : false
end
if speed > 60 ? true : false
end
end
